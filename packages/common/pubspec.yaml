name: common
description: Common package pet-in project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.1.5 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  #Firebase
  firebase_core: ^2.31.0
  firebase_crashlytics: ^3.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  very_good_analysis: ^5.1.0

flutter:
